[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "octobot-trading"
version = "2.5.13"
description = "OctoBot project trading package"
readme = "README.md"
license = "LGPL-3.0"
requires-python = ">=3.13"
authors = [
    { name = "Drakkar-Software", email = "contact@drakkar.software" },
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX",
]
dependencies = [
    "numpy==1.26.3",
    "octobot-backtesting>=1.10,<1.11",
    "async-channel>=2.2,<2.3",
    "octobot-commons>=1.10,<1.11",
    "octobot-tentacles-manager>=2.10,<2.11",
    "trading-backend>=1.2.35",
    "ccxt==4.5.28",
    "cryptography",
    "sortedcontainers==2.4.0",
    "cachetools>=5.5.0,<6",
]

[project.optional-dependencies]
full = [
    "octobot-backtesting[full]>=1.10,<1.11",
    "octobot-commons[full]>=1.10,<1.11",
    "octobot-tentacles-manager[full]>=2.10,<2.11",
    "tinydb==4.5.2",
]

[project.urls]
Homepage = "https://github.com/Drakkar-Software/OctoBot-Trading"

[dependency-groups]
dev = [
    "pytest>=9.0",
]

[tool.uv]
default-groups = ["dev"]

[tool.hatch.build.targets.wheel]
packages = ["src/octobot_trading"]

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "C4", "RUF", "PERF", "TC", "PTH"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]
"__init__.py" = ["F401"]

[tool.ruff.lint.isort]
known-first-party = ["octobot_trading"]

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "basic"

[tool.pytest.ini_options]
minversion = "9.0"
addopts = ["-ra", "-q", "--strict-markers", "--import-mode=importlib"]
testpaths = ["tests"]
pythonpath = ["src"]
xfail_strict = true
filterwarnings = ["error"]
